import React from "react";
import {Typography} from "@material-ui/core";

export const UploadVideoSuccessPage = () => {
    return <div className={"padding-md"}>
        <Typography variant={"h1"}>
            Upload success!
        </Typography>
    </div>
}
